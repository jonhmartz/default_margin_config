<odoo>
    <record id="view_margin_config_form" model="ir.ui.view">
        <field name="name">default.margin.config.form</field>
        <field name="model">default.margin.config</field>
        <field name="arch" type="xml">
            <form string="Configuración de Utilidad">
                <sheet>
                    <group>
                        <field name="utilidad"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_margin_config_tree" model="ir.ui.view">
        <field name="name">default.margin.config.tree</field>
        <field name="model">default.margin.config</field>
        <field name="arch" type="xml">
            <tree>
                <field name="utilidad"/>
            </tree>
        </field>
    </record>

    <menuitem id="menu_margin_config_root" name="Utilidad" sequence="10"/>
    <menuitem id="menu_margin_config" name="Configuración de Utilidad" parent="menu_margin_config_root" action="action_margin_config"/>

    <record id="action_margin_config" model="ir.actions.act_window">
        <field name="name">Configuración de Utilidad</field>
        <field name="res_model">default.margin.config</field>
        <field name="view_mode">tree,form</field>
        <field name="type">ir.actions.act_window</field>
        <field name="help" type="html">
            <p>Define el porcentaje de utilidad predeterminado que se aplicará a las cotizaciones y ventas.</p>
        </field>
    </record>
</odoo>
